# ============================================================
# 生产环境覆盖配置
# 由 config.ts 在 NODE_ENV === 'production' 时加载
# 部署时请通过真实环境变量注入 DB_PASS / WECHAT_SECRET 等敏感信息
# ============================================================

# 显式开启 TLS 校验（Node 默认即校验，这里写出来仅作文档说明）
NODE_TLS_REJECT_UNAUTHORIZED=1
